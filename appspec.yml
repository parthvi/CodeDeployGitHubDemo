version: 0.0
os: linux
files:
  - source: /
    destination: /var/www/topdust
permissions:
  - object: /var/www/topdust
    pattern: "**"
    owner: root
    group: root
    mode: 755
    type:
      - file
